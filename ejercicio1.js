const producto = {
    artículo: "Laptop",
    precio: 900000,
    disponible: true,
}
console.log(`El artículo ${producto.artículo} tiene un precio de $${producto.precio} y está disponible: ${producto.disponible}`);